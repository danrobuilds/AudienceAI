langchain>=0.1.0
langchain-community>=0.0.40
ollama>=0.2.0 # For ChatOllama and OllamaEmbeddings
pandas>=2.0.0 # For vector.py data handling
scikit-learn>=1.3.0 # For the previous TF-IDF, but Chroma in vector.py might have its own vector math needs or this is a leftover. Let's keep for now.
chromadb-client>=0.4.22 # Dependency for langchain_chroma
requests>=2.31.0 # General HTTP, good to have though LangChain handles most direct LLM comms

# Note: Ensure ollama (the application) is installed and running on your system. 